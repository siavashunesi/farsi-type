farsi-type
==========

input limiter to persian and english langauge with **Jquery**

#usage
```javascript
$(function(){
  FarsiType.init($('form'));
  /** or **/
  FarsiType.init($('body'));
});
```
